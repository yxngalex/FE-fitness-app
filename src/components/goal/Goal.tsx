import {DialogFooter} from "@/components/ui/dialog.tsx";
import {Button} from "@/components/ui/button.tsx";

const Goal = () => {

    return (
        <>
            <h2>Goal Dialog</h2>
            <DialogFooter>
                <Button>Save</Button>
            </DialogFooter>
        </>
    );
}

export default Goal;